# تحليل أولي لمسار البحث عن الروابط

## المسار الحالي

يبدأ البحث من `src/handlers/linksMenu.js:handleLinksExecuteSearch`، حيث يُنشأ سجل العملية ثم يُشغّل `linksService.runSearch` كسلسلة Promise داخل عملية البوت. حالة التشغيل الحية محفوظة في `activeSearches` داخل الذاكرة، بينما تحفظ قاعدة البيانات عدادات عامة فقط. لا توجد Queue مستقلة أو آلية استعادة عند إعادة تشغيل العملية.

داخل `src/services/linksService.js` تُحمّل الحسابات ثم المحادثات عبر `client.getDialogs({ limit: 500 })`. لكل محادثة يُستدعى `client.getMessages(dialog.entity, { limit: msgLimit })` مرة واحدة فقط. لذلك فإن `fast`, `medium`, و`deep` هي حدود عدد رسائل مفردة وليست حدودًا قابلة للتحقق عبر Pagination. يتم تطبيق الفترة الزمنية بعد الجلب، ما يعني أن الوصول إلى حد `limit` قد يمنع الوصول إلى أقدم تاريخ مطلوب.

## الأسباب الجذرية المرصودة

1. لا توجد Pagination مستمرة؛ الاستدعاء الواحد لـ `getMessages` لا يضمن فحص كامل النطاق.
2. `DEEP_MESSAGE_LIMIT` يجعل البحث العميق محدودًا بعدد ثابت، خلافًا لمتطلب استنفاد النطاق الزمني.
3. أخطاء قراءة أي محادثة تُسجّل ثم تُتجاهل، وبعدها يمكن إعلان العملية `completed` رغم وجود نطاقات غير مفحوصة.
4. لا يوجد Checkpoint دائم لكل مصدر/صفحة، ولا Resume بعد Timeout أو FloodWait أو Crash أو Restart.
5. `activeSearches` و`pause/resume/stop` في RAM فقط؛ إغلاق البوت أو فقدان الواجهة يفقد التحكم والحالة التشغيلية.
6. الحفظ يتم رابطًا رابطًا مع استعلام duplicate قبل كل إدراج، ولا توجد Unique Constraint على `(user_id, url_hash)` أو هوية مناسبة. `INSERT OR IGNORE` لا يمنع التكرار حاليًا لأن القيد الفريد غير موجود.
7. الاستخراج يفحص `msg.message` فقط ولا يفحص Message Entities، كما أن التطبيع محدود بخفض الحالة والـ trim.
8. المؤقت `withTimeout` يرفض الـ Promise لكنه لا يلغي طلب Telegram الأصلي، مما قد يترك طلبًا متأخرًا أو اتصالًا متداخلًا.
9. لا توجد حالات تشغيل تفصيلية مثل `paused_rate_limit`, `stalled`, `resuming` ولا سبب اكتمال موثق.
10. التحديثات الدورية تكتب العدادات، لكنها لا تحفظ cursor أو آخر message id أو صفحات مكتملة، ولا توجد مؤشرات retry/flood/error.

## اتجاه الإصلاح

سيُبنى الإصلاح حول Fetch Page متسلسل لكل محادثة مع cursor/offset_id، ثم معالجة دفعة الرسائل واستخراج كل الروابط، ثم حفظ idempotent داخل Transaction، وبعد نجاح الحفظ فقط يُحفظ checkpoint. ستُصنّف الأخطاء وتُعاد المحاولة مع backoff، ويُعامل FloodWait كحالة توقف مؤقت قابلة للاستئناف. ستظل واجهة البوت متوافقة، مع جعل حالة التشغيل قابلة للقراءة من قاعدة البيانات.

## ملاحظة الاختبار

الاختبارات الحالية تغطي استخراج بعض الأنماط وملفات النتائج فقط، ولا تختبر Pagination أو Resume أو FloodWait أو فشل قاعدة البيانات أو استكمال البحث بعد انقطاع الشبكة. لذلك ستُضاف اختبارات وحدة/تكامل لمحرك pagination والتطبيع والاستئناف، مع الحفاظ على اختبار ملفات النتائج الحالي.
