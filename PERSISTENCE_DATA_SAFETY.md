# حماية واستمرارية بيانات البوت

## نطاق المراجعة

تم فحص جداول المستخدمين، أكواد التفعيل، عمليات الاسترداد، الاشتراكات، الحسابات، الجلسات المشفرة، نظام الاستعادة عند بدء التشغيل، مسارات حذف الحسابات، ومهام التنظيف المؤقتة. قاعدة البيانات الحالية تستخدم SQLite مع migrations إضافية، وتحتوي مسبقًا على UPSERT للمستخدمين والحسابات حسب `(user_id, phone)`، وتخزن `encrypted_session` داخل جدول الحسابات.

## النسخة الاحتياطية قبل التعديل

تم إنشاء snapshot للمستودع قبل التعديل عند commit `a3eee658e62545d8635ca63523ed8f3b38adf9be` في:

`/home/ubuntu/telegram-bot-backups/20260817T043057Z`

لم توجد ملفات قاعدة بيانات أو جلسات runtime داخل نسخة المستودع وقت التنفيذ، لأن هذه الملفات مستبعدة من Git ومفترض وجودها على volume persistent في بيئة التشغيل.

## التغييرات

أضيفت migration رقم 5 لإنشاء `persistence_audit_logs`، مع تسجيل عمليات إنشاء/تحديث المستخدمين، تفعيل وتعطيل وانتهاء التفعيل، UPSERT للحسابات، إعادة الاتصال، حفظ الجلسات، استعادة ملفات الجلسات، وحذفها الصريح.

أضيفت migration رقم 6 لإنشاء unique partial index على `(user_id, telegram_id)` للحسابات التي تملك هوية Telegram مؤكدة. قبل إنشاء القيد يتم فحص التكرارات؛ إذا وجدت سجلات متكررة، تتوقف migration برسالة واضحة بدل حذف أو دمج البيانات تلقائيًا.

أضيف backup تلقائيًا قبل تهيئة قاعدة بيانات موجودة، وbackup إضافيًا عند بدء البوت. عند فشل النسخة الاحتياطية قبل التهيئة يتوقف فتح قاعدة البيانات لحماية البيانات. وعند فشل migration يتم إغلاق الاتصال وإعادة رمي الخطأ، فلا يستمر النظام بقاعدة نصف مهيأة.

الجلسات تبقى مشفرة في قاعدة البيانات، ويعاد إنشاء ملفاتها بصلاحيات `0600` عند الحاجة. فشل الاتصال أو انتهاء الجلسة يغير الحالة فقط ولا يحذف سجل الحساب أو `encrypted_session`. حذف الحساب وملف الجلسة لا يحدث إلا عبر مسار الحذف الصريح الموجود في لوحة الإدارة، ويتم تسجيله في audit log.

## التحقق

تم تشغيل syntax checks، واختبار الاستمرارية بعد محاكاة عمليتي كتابة وإعادة تشغيل منفصلتين، واختبارات activation guard وsession restore واختبارات البحث الحالية. اختبار restart أثبت بقاء المستخدم، التفعيل المفتوح، redemption، الحساب بنفس المعرف، الهوية، الجلسة المشفرة، وإمكانية إعادة إنشاء ملف الجلسة بعد إعادة التشغيل.
