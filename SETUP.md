# إعداد وتشغيل البوت

## التثبيت

```bash
git clone https://github.com/a781780889-rgb/Telegram-bot-updated.git
cd Telegram-bot-updated
npm install
cp .env.example .env
nano .env
```

يجب ضبط `BOT_TOKEN` و`API_ID` و`API_HASH` و`ENCRYPTION_KEY` قبل التشغيل. يجب أن تكون قيمة `ENCRYPTION_KEY` ثابتة، لأن تغييرها يمنع فك تشفير جلسات Telegram المحفوظة.

## التخزين الدائم

بيانات المستخدمين والاشتراكات والحسابات والمهام محفوظة في SQLite، بينما تحفظ جلسات Telegram مشفّرة داخل قاعدة البيانات، مع نسخة ملف جلسة على القرص لإعادة الاتصال السريع. في الإنتاج يجب أن تكون هذه المسارات على Volume دائم خارج مجلد الإصدار:

```env
PERSISTENT_DATA_DIR=/data
DATABASE_PATH=/data/database/bot.db
SESSIONS_PATH=/data/sessions
BACKUP_PATH=/data/backups
```

الأسماء القديمة `DB_PATH` و`SESSIONS_DIR` و`DATA_BACKUP_DIR` ما زالت مدعومة للتوافق مع التثبيتات الحالية. إذا عُرّف `DATABASE_PATH` فإنه يصبح مصدر الحقيقة الأول لمسار قاعدة البيانات، ولا ينشئ التطبيق قاعدة بديلة في مجلد المشروع.

في التشغيل المحلي يمكن استخدام:

```env
PERSISTENT_DATA_DIR=./data
DATABASE_PATH=./data/database/bot.db
SESSIONS_PATH=./data/sessions
BACKUP_PATH=./data/backups
```

لا تضع بيانات الإنتاج داخل مجلد يتم استبداله عند النشر، ولا تحفظ ملف `.env` أو الجلسات في GitHub.

## حماية التحديثات

عند بدء التشغيل، إذا كانت قاعدة البيانات موجودة، تُستخدم كما هي وتُنشأ نسخة احتياطية قبل تهيئة المخطط. الترحيلات تراكمية وآمنة ولا تستخدم `DROP TABLE` أو إعادة إنشاء قاعدة البيانات. بعد الترحيل يتحقق التطبيق من بقاء الاشتراكات النشطة وكل حسابات Telegram والجلسات المشفّرة. إذا فشل التحقق، يتوقف التشغيل بدل تشغيل نسخة قد تسبب فقدان البيانات.

ملفات الجلسات المفقودة يمكن إعادة إنشائها من عمود `encrypted_session` الموجود في قاعدة البيانات. لذلك يجب الحفاظ على قاعدة البيانات ومسار `ENCRYPTION_KEY` معًا.

## النشر الآمن

يُجهّز الإصدار الجديد ويُختبر أولًا، ثم يُنشر مع إبقاء Volume الدائم متصلًا. لا تحذف مجلد البيانات أثناء تحديث الكود. قبل أي Migration تُنشأ نسخة احتياطية في `BACKUP_PATH`. في حال فشل الإصدار، أعد إصدار الكود السابق مع إبقاء Volume وقاعدة البيانات والجلسات دون تغيير.

## التشغيل

```bash
node src/index.js
```

عند إعادة التشغيل، يحمّل البوت الحسابات التي تحتوي على جلسة مشفّرة ويحاول استعادتها دون طلب OTP جديد. فشل استعادة حساب واحد لا يحذف الحساب ولا الجلسة؛ بل يسجل الحالة ويترك الحساب محفوظًا لإعادة المحاولة أو إعادة تسجيل الدخول صراحةً.
